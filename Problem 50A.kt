// https://codeforces.com/problemset/problem/50/A
// Domino piling

fun main() {
    val line = readln().split(" ") // read line from the input, save numbers only
    println(line[0].toInt() * line[1].toInt() / 2) // print the result of div by 2
}
